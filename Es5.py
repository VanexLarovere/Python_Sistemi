import turtle #Importo una libreria built in (Nativo), modulo che implementa il linguaggio logo. 

finestra = turtle.Screen() #creazione finestra
rebecca = turtle.Turtle() #creazione tartaruga
rebecca.forward(100)
rebecca.left(30)
rebecca.forward(50)

finestra.mainloop() #mantiene la finestra aperta